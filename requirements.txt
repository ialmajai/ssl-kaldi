# Shared dependencies for the audio recipes (Python 3.8+).
# The GRID lipreading recipe additionally needs AV-HuBERT / fairseq / dlib —
# see egs/grid/s5/requirements.txt.
# For a reproducible install, use the pinned lock: pip install -r requirements.lock
numpy>=1.22,<2.0
torch>=2.4.0
transformers>=4.46.0
torchdr>=0.3.0
matplotlib>=3.5
kaldiio>=2.18.0
