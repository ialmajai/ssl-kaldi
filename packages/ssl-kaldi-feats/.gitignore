# Build artifacts. Regenerated by `python -m build`, and the sdist alone is
# ~600 KB of binary that would land in history on every release.
dist/
build/
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/
